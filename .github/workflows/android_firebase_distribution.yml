name: Android Fastlane Firebase Distribution

on:
  workflow_dispatch:

  push:
    branches:
      - main

concurrency:
  group: firebase-distribution
  cancel-in-progress: true

jobs:
  distribute:
    name: Build & Distribute APK
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 17

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true

      - name: Setup Ruby
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: "3.2"
<<<<<<< HEAD
=======
          bundler-cache: true
>>>>>>> cec7fcb63b3c7895378e7bb3969d1abf66c0e00c

      - name: Install Flutter Dependencies
        run: flutter pub get

      - name: Verify Flutter Installation
        run: flutter doctor -v

<<<<<<< HEAD
      # 🔥 IMPORTANT: install gems inside android folder (where Fastlane is)
      - name: Install Ruby Gems
        working-directory: android
        run: bundle install

=======
>>>>>>> cec7fcb63b3c7895378e7bb3969d1abf66c0e00c
      - name: Run Fastlane Firebase Distribution
        working-directory: android
        env:
          FIREBASE_CLI_TOKEN: ${{ secrets.FIREBASE_CLI_TOKEN }}
          FIREBASE_APP_ID: ${{ secrets.FIREBASE_APP_ID }}
        run: bundle exec fastlane firebase_distribution