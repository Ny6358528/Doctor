name: Android Fastlane Firebase Distribution

on:
  workflow_dispatch:

  push:
    branches:
      - main

concurrency:
  group: firebase-distribution
  cancel-in-progress: true

jobs:
  distribute:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      # Java setup
      - name: Setup Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 17

      # Flutter setup
      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true

      # Ruby setup
      - name: Setup Ruby
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: "3.2"

      # Flutter dependencies
      - name: Install Flutter Dependencies
        run: flutter pub get

<<<<<<< HEAD
      # 🔥 Install Fastlane dependencies (IMPORTANT)
=======
      # 🔥 تثبيت Ruby + Fastlane gems
>>>>>>> 1388b265e8f3f75445c42737327195e11dd393b4
      - name: Install Ruby Dependencies
        working-directory: android
        run: bundle install

<<<<<<< HEAD
      # 🚀 Run Fastlane (NO install_plugins step)
      - name: Run Fastlane Firebase Distribution
=======
      # 🔥 مهم جدًا: تحميل plugins
      - name: Install Fastlane Plugins
        working-directory: android
        run: bundle exec fastlane install_plugins

      # 🚀 تشغيل Fastlane
      - name: Run Fastlane
>>>>>>> 1388b265e8f3f75445c42737327195e11dd393b4
        working-directory: android
        env:
          FIREBASE_APP_ID: ${{ secrets.FIREBASE_APP_ID }}
          FIREBASE_CLI_TOKEN: ${{ secrets.FIREBASE_CLI_TOKEN }}
        run: bundle exec fastlane firebase_distribution