name: Android Firebase App Distribution

on:
  workflow_dispatch:

  push:
    branches:
      - main

concurrency:
  group: firebase-distribution
  cancel-in-progress: true

jobs:
  distribute:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 17

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true

      - name: Setup Ruby
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: "3.3"
          bundler-cache: true

      - name: Install Flutter Dependencies
        run: flutter pub get

      - name: Install Ruby Dependencies
        working-directory: android
        run: bundle install --jobs 4 --retry 3

<<<<<<< HEAD
=======
      # ✅ مهم: نحول JSON من Secret إلى ملف فعلي
>>>>>>> df7620345662a42474582cfcc7f05c33bbb43c9c
      - name: Create Service Account File
        working-directory: android
        run: |
          echo '${{ secrets.FIREBASE_SERVICE_ACCOUNT }}' > service-account.json

<<<<<<< HEAD
      # تشخيص الملف
      - name: Check File Exists
        working-directory: android
        run: |
          ls -lah
          wc -c service-account.json

      # التحقق أن JSON صالح
      - name: Validate JSON
        working-directory: android
        run: |
          python3 -m json.tool service-account.json > /dev/null
          echo "JSON VALID"

      # عرض بعض الحقول للتأكد فقط
      - name: Show Service Account Info
        working-directory: android
        run: |
          python3 -c "
          import json
          f=json.load(open('service-account.json'))
          print('type=',f.get('type'))
          print('project_id=',f.get('project_id'))
          print('client_email=',f.get('client_email'))
          "

=======
>>>>>>> df7620345662a42474582cfcc7f05c33bbb43c9c
      - name: Run Fastlane
        working-directory: android
        env:
          FIREBASE_APP_ID: ${{ secrets.FIREBASE_APP_ID }}
        run: bundle exec fastlane firebase_distribution